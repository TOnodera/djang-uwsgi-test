{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run uwsgi",
            "type": "shell",
            "command": "uwsgi --socket :8001 --chdir /home/python/app/config --module config.wsgi:application"
        }
    ]
}